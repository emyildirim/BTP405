use db;

-- CREATE TABLE IF NOT EXISTS Note (
--     id INT AUTO_INCREMENT PRIMARY KEY,
--     title VARCHAR(128) NOT NULL,
--     content TEXT NOT NULL
-- );

-- INSERT INTO Note(title, content)
-- VALUES("Demo Title", "Some Content"), ("Title2", "Other Content");